# ── Database PostgreSQL ─────────────────────────────
DB_HOST=astro-dev-db
DB_PORT=5432
DB_NAME=astrologia
DB_USER=astro
DB_PASS=astro2024secure

# ── Ambiente applicativo ────────────────────────────
APP_ENV=development
APP_DEBUG=true

# ── Postgres container init (solo per docker-compose) ─
POSTGRES_USER=astro
POSTGRES_PASSWORD=astro2024secure
POSTGRES_DB=astrologia
